1- Create variables name, target_level, and target_year. Print: "I am [name], aiming for [level] in Japan by [year]."

2- Store a price as a float, a quantity as an int, and a product name as a string. Print the total cost using an f-string

3- Create a list of 3 Japanese cities. Add a 4th city to the end and print only the second city in the list.

4- Write an if-else statement: If n3_score is 95 or higher, print "Pass", otherwise print "Study Harder".

5- Ask the user for their favorite programming language. If they type "Python", print "Great for DSA!"; otherwise, print "Good choice!"

6- Create a list of numbers [1, 2, 3, 4, 5]. Loop through them and print the square ($n^2$) of each number.

7- Create a variable countdown = 5. Use a while loop to print the number and decrease it until it hits 0.

8- Write a function greet_dev(name, language) that returns: "Hello [name], welcome to [language] programming!"

9- Create a product dictionary with keys: id, name, and price. Print only the price.

10- Add a new key stock to the dictionary from Q9 and update the price to a new value. Print the updated dictionary.

11- You have prices = [100, 200, 300, 400]. Use a List Comprehension to create a new list discounted_prices where each price is 10% off.

12- Write a try-except block. Ask the user for a number. If they type a string (like "abc"), catch the ValueError and print "Invalid input! Please enter a number."

13- Create a list of two dictionaries, where each dictionary represents a user (name and email). Access and print the name of the second user.

14- Create a string alphabet = "abcdefghij". Use slicing to print only the first 5 characters and then only the last 3.

15- Create a list with duplicate values: [1, 2, 2, 3, 4, 4, 5]. Convert it to a Set to remove duplicates and print the result.